<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chats</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <h2>All chats</h2>
    <a href="/chats/new">create a new chat</a><br><br>
    <% for(let chat of chats) { %>
    <div class="chat">
        <p>From: <i><%= chat.from %></i> </p>
        <p class="message"> <%= chat.message %></p>
        <div class="links">
            <a href="http://127.0.0.1:3000/chats/<%=chat.id%>" >show</a> &nbsp;
            <a href="http://127.0.0.1:3000/chats/<%=chat.id%>/edit" >edit</a> &nbsp;
            <form action="/chats/<%=chat.id%>?_method=DELETE" method="post">
              <button>Delete</button>
            </form>
        </div>
        <p>Received by: <i><%= chat.to %></i> </p>
        <hr> 
        <p><%= chat.created_at.toString().split(" ")[4] %> <br>
            <%= chat.created_at.toString().split(" ").slice(0,4).join(" ") %> <br>
        </p>
        
    </div>
    <br>
    <% } %>
    <br>
    
</body>
</html>