<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Data</title>
    <style>
        table,th, td{
            border: 1px solid black; /* Sets border width, style, and color */
            border-collapse: collapse; /* Merges adjacent borders into a single border */
            padding: 8px; /* Adds space between the content and the border */
        }
        th {
          background-color: #f2f2f2; /* Optional: adds background color to header cells */
        }
    </style>
</head>
<body>
    <div>
        <table>
            <caption>Users Information</caption>
            <tr>
                <th>User Id</th>
                <th>Username</th>
                <th>email</th>
            </tr>
            <% for(user of users){ %> 
            <tr>
                <td> <%= user.id %></td>
                <td> <%= user.username %></td>
                <td> <%= user.email %></td>
                <td> <a href="http://localhost:3000/user/<%= user.id%>/edit">edit</a></td>
            </tr>
            <% } %>
        </table>
    </div>
    
</body>
</html>



